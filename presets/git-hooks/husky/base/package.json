{
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{md,html,css}": [
      "prettier --write"
    ],
    "*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ]
  },
  "devDependencies": {
    "husky": "0.0.0",
    "lint-staged": "0.0.0"
  }
}
