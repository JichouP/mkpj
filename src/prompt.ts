import inquirer from 'inquirer';

// eslint-disable-next-line @typescript-eslint/explicit-module-boundary-types
export const prompt = () => inquirer.prompt([{ type: '' }]);

/*
npm
typescript
webpack
git-hooks
prettier
eslint
jest
*/
